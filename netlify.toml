# Netlify 배포 설정
[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# SPA 라우팅 - 모든 경로를 index.html로
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
